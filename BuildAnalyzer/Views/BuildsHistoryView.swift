//
//  BuildsHistoryView.swift
//  BuildAnalyzer
//
//  Created by Bartosz Polaczyk on 6/19/23.
//

import Foundation
import SwiftUI

struct BuildsHistoryView: View {
    var body: some View {
        HStack {
            Button<Text>("Load") {
//                let manifest = wk.rebuild()
//                items = (try? producer.build(manifest)) ?? []
//                filteredItems = producer.filter(string: search, elements: items)
            }
            Button<Text>("1") {
//                wk.send("a -> b; b -> c;")
            }
        }
    }
}
